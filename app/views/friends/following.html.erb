FOLLOWING
<% @records.each do |record| %>
<% user = User.find(record.toUser) %>
<% if user != nil %>
<p> <%= user.name %> <%= user.email %> <%= render "follow", touser: record.toUser %>
<%end%>
<%end%>
