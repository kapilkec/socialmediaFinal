
<%= stylesheet_link_tag 'friendsFollow' %>

 <% follow_check = Friend.where(fromUser:1,toUser:touser) %>
 <% if follow_check.length == 0 %>
 <%= button_to "Follow", set_follow_path, params: {friend: {fromUser:1,toUser: touser}},class:"btn btn-success btn-sm", method: :post %>
 <% else %>

  <%= button_to "Unfollow", set_unfollow_path(follow_check[0]),class:"btn btn-danger btn-sm",  method: :delete %>
<% end %>

